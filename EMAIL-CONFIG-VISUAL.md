# Email Configuration System Architecture

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  admin-email-config.html                                        │
│  ┌───────────────────────────────────────────────────────┐    │
│  │  1. Choose Provider (Visual Cards)                    │    │
│  │     [Gmail] [M365] [SendGrid] [Mailgun] [SES] [Custom]│   │
│  │                                                         │    │
│  │  2. Enter Credentials                                   │    │
│  │     Email: ___________________                          │    │
│  │     Password: _______________                           │    │
│  │     From Name: _______________                          │    │
│  │                                                         │    │
│  │  3. [Save Configuration] [Test Email]                   │    │
│  └───────────────────────────────────────────────────────┘    │
│                                                                 │
└────────────────────┬───────────────────────────────────────────┘
                     │ HTTP POST
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                          API LAYER                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  routes/email-providers.php                                     │
│  ┌───────────────────────────────────────────────────────┐    │
│  │                                                         │    │
│  │  GET /api/email/providers                               │    │
│  │  ├─→ List all available email providers                │    │
│  │                                                         │    │
│  │  GET /api/email/providers/{key}                         │    │
│  │  ├─→ Get specific provider configuration              │    │
│  │                                                         │    │
│  │  POST /api/email/apply-preset                           │    │
│  │  ├─→ Save email configuration                          │    │
│  │  ├─→ Encrypt password (AES-256-CBC)                    │    │
│  │  └─→ Store in database                                 │    │
│  │                                                         │    │
│  └───────────────────────────────────────────────────────┘    │
│                                                                 │
└────────────────────┬───────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE LAYER                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  email_provider_presets                                         │
│  ┌───────────────────────────────────────────────────────┐    │
│  │ id │ provider_key │ provider_name │ mail_host │ ...   │    │
│  ├────┼──────────────┼───────────────┼───────────┼────────┤   │
│  │ 1  │ gmail        │ Gmail         │ smtp.gmail.com     │    │
│  │ 2  │ microsoft365 │ Microsoft 365 │ smtp.office365.com │    │
│  │ 3  │ sendgrid     │ SendGrid      │ smtp.sendgrid.net  │    │
│  │ 4  │ mailgun      │ Mailgun       │ smtp.mailgun.org   │    │
│  │ 5  │ amazon_ses   │ Amazon SES    │ email-smtp.*.com   │    │
│  │ 6  │ custom       │ Custom SMTP   │ (user defined)     │    │
│  └───────────────────────────────────────────────────────┘    │
│                                                                 │
│  email_config (Stores Active Configuration)                    │
│  ┌───────────────────────────────────────────────────────┐    │
│  │ id │ provider_key │ mail_username │ mail_password │   │    │
│  ├────┼──────────────┼───────────────┼───────────────┤       │
│  │ 1  │ gmail        │ user@gmail.com│ [ENCRYPTED]   │       │
│  └───────────────────────────────────────────────────────┘    │
│                                                                 │
└────────────────────┬───────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SERVICE LAYER                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  src/Services/EmailService.php                                  │
│  ┌───────────────────────────────────────────────────────┐    │
│  │                                                         │    │
│  │  loadConfiguration()                                    │    │
│  │  ├─→ Read from email_config table                      │    │
│  │  ├─→ Decrypt password                                  │    │
│  │  └─→ Configure PHPMailer                               │    │
│  │                                                         │    │
│  │  sendEmail()                                            │    │
│  │  ├─→ Use configured SMTP settings                      │    │
│  │  ├─→ Load template from email_templates                │    │
│  │  ├─→ Send via PHPMailer                                │    │
│  │  └─→ Log to email_logs                                 │    │
│  │                                                         │    │
│  │  sendPasswordResetEmail()                               │    │
│  │  sendWelcomeEmail()                                     │    │
│  │  sendTestEmail()                                        │    │
│  │                                                         │    │
│  └───────────────────────────────────────────────────────┘    │
│                                                                 │
└────────────────────┬───────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                    EXTERNAL SMTP SERVERS                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [Gmail SMTP]           smtp.gmail.com:587 (TLS)                │
│  [Microsoft 365 SMTP]   smtp.office365.com:587 (TLS)            │
│  [SendGrid SMTP]        smtp.sendgrid.net:587 (TLS)             │
│  [Mailgun SMTP]         smtp.mailgun.org:587 (TLS)              │
│  [Amazon SES SMTP]      email-smtp.region.amazonaws.com:587     │
│  [Custom SMTP]          user-defined                            │
│                                                                 │
└────────────────────┬───────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                       EMAIL RECIPIENTS                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  👤 Users receiving:                                            │
│     • Password reset links                                      │
│     • Welcome emails                                            │
│     • System notifications                                      │
│     • Test emails                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow for Sending Email

```
┌────────────────────────────────────────────────────────────────┐
│ STEP 1: User Action                                            │
└───┬────────────────────────────────────────────────────────────┘
    │ User clicks "Forgot Password" or Admin creates user
    ▼
┌────────────────────────────────────────────────────────────────┐
│ STEP 2: Application Trigger                                    │
├────────────────────────────────────────────────────────────────┤
│ routes/auth.php:                                               │
│   $emailService->sendPasswordResetEmail($email, $name, $token) │
└───┬────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ STEP 3: EmailService Loads Configuration                      │
├────────────────────────────────────────────────────────────────┤
│ EmailService::loadConfiguration()                             │
│   1. Query email_config table                                 │
│   2. Get provider_key (e.g., "gmail")                         │
│   3. Decrypt password using ENCRYPTION_KEY                    │
│   4. Configure PHPMailer with SMTP settings                   │
└───┬────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ STEP 4: Load Email Template                                   │
├────────────────────────────────────────────────────────────────┤
│ EmailService::getTemplate('password_reset')                   │
│   1. Query email_templates table                              │
│   2. Get subject and body_html                                │
│   3. Substitute variables: {{name}}, {{token}}, {{url}}       │
└───┬────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ STEP 5: Send via PHPMailer                                    │
├────────────────────────────────────────────────────────────────┤
│ $mailer->send()                                                │
│   1. Connect to smtp.gmail.com:587                            │
│   2. Authenticate with username/password                      │
│   3. Send email with TLS encryption                           │
│   4. Return success/failure                                   │
└───┬────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ STEP 6: Log Result                                            │
├────────────────────────────────────────────────────────────────┤
│ EmailService::logEmail()                                       │
│   INSERT INTO email_logs                                       │
│   (recipient, subject, status, sent_at)                        │
│   VALUES (user@example.com, 'Reset Password', 'sent', NOW())  │
└───┬────────────────────────────────────────────────────────────┘
    │
    ▼
┌────────────────────────────────────────────────────────────────┐
│ STEP 7: User Receives Email                                   │
├────────────────────────────────────────────────────────────────┤
│ 📧 Email delivered to inbox                                    │
│ 🔒 Contains password reset link with token                    │
│ ⏱️ Link expires in 1 hour                                      │
└────────────────────────────────────────────────────────────────┘
```

## Security Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                       SECURITY LAYERS                           │
└─────────────────────────────────────────────────────────────────┘

Layer 1: TRANSPORT SECURITY
    ├─ HTTPS for all admin pages
    ├─ TLS/SSL for SMTP connections
    └─ No credentials sent in plain text

Layer 2: AUTHENTICATION
    ├─ Admin login required for email config
    ├─ Session-based access control
    └─ API key for programmatic access

Layer 3: ENCRYPTION AT REST
    ├─ Password encrypted with AES-256-CBC
    ├─ Unique IV (Initialization Vector) per password
    ├─ Encryption key stored in .env (not in Git)
    └─ Encryption flow:
          Plain Password
               ↓
          [AES-256-CBC] + ENCRYPTION_KEY + Random IV
               ↓
          Base64(IV + Encrypted Data)
               ↓
          Stored in Database

Layer 4: PASSWORD HANDLING
    ├─ App passwords preferred (not main passwords)
    ├─ Gmail: 16-character app password
    ├─ Microsoft: App-specific password with 2FA
    └─ Never log or display passwords in UI

Layer 5: DATABASE SECURITY
    ├─ email_config table access restricted
    ├─ Foreign key constraints
    ├─ Audit trail via updated_by column
    └─ No direct password decryption in queries

Layer 6: APPLICATION SECURITY
    ├─ Input validation (email format, required fields)
    ├─ SQL injection protection (prepared statements)
    ├─ XSS protection (escaped output)
    └─ Error handling (no sensitive info in errors)
```

## Provider Selection Logic

```
┌────────────────────────────────────────────────────────────────┐
│ How Provider Selection Works                                   │
└────────────────────────────────────────────────────────────────┘

IF user selects "Gmail":
    ├─ Auto-fill: mail_host = "smtp.gmail.com"
    ├─ Auto-fill: mail_port = 587
    ├─ Auto-fill: mail_encryption = "tls"
    ├─ Show: Gmail-specific instructions
    └─ Require: Email + App Password

ELSE IF user selects "Microsoft 365":
    ├─ Auto-fill: mail_host = "smtp.office365.com"
    ├─ Auto-fill: mail_port = 587
    ├─ Auto-fill: mail_encryption = "tls"
    ├─ Show: Microsoft-specific instructions
    └─ Require: Email + Password (or App Password)

ELSE IF user selects "Custom":
    ├─ Show: Custom host input field
    ├─ Show: Custom port input field
    ├─ Show: Custom encryption dropdown
    ├─ Require: All SMTP details
    └─ No auto-fill (user provides everything)

ELSE (SendGrid, Mailgun, Amazon SES):
    ├─ Auto-fill: provider-specific SMTP settings
    ├─ Show: Provider-specific instructions
    └─ Require: API credentials or SMTP username/password
```

## Files and Their Purpose

```
┌─────────────────────────────────────────────────────────────────┐
│ File Structure                                                  │
└─────────────────────────────────────────────────────────────────┘

Database Schema:
  sql/add-email-provider-presets.sql
    └─ Creates email_provider_presets table
    └─ Inserts 6 default providers
    └─ Adds columns to email_config table

Backend (PHP):
  routes/email-providers.php
    └─ API endpoints for provider management
  
  src/Services/EmailService.php (existing)
    └─ Email sending logic
    └─ Configuration loading
    └─ Password encryption/decryption

  public/index.php
    └─ Loads email-providers routes

Frontend (HTML/JS):
  public/admin-email-config.html
    └─ Provider selection UI
    └─ Configuration form
    └─ Test email functionality

Configuration:
  .env
    └─ Encryption key
    └─ Fallback email settings

Documentation:
  EMAIL-CONFIG-DEPLOYMENT.md → Full deployment guide
  EMAIL-QUICK-SETUP.md → 5-minute setup for Gmail
  EMAIL-CONFIG-SUMMARY.md → Feature overview
  EMAIL-CONFIG-VISUAL.md → This file (architecture diagrams)

Deployment:
  deploy-email-config.sh
    └─ Automated deployment script
```

## Integration Points

```
┌─────────────────────────────────────────────────────────────────┐
│ How Email System Integrates with Existing Code                 │
└─────────────────────────────────────────────────────────────────┘

routes/auth.php (Password Reset):
    Line 382: $emailService->sendPasswordResetEmail(...)
               ↓
           Uses email_config from database
               ↓
           Sends email via configured SMTP

routes/auth.php (New User):
    Line 620: $emailService->sendWelcomeEmail(...)
               ↓
           Uses welcome email template
               ↓
           Sends via configured provider

admin-email-settings.html (Test Email):
    Test button → POST /api/email/test
                   ↓
               EmailService->sendTestEmail()
                   ↓
               Verifies configuration works

src/Services/EmailService.php (Constructor):
    __construct(PDO $pdo, string $encryptionKey)
         ↓
    loadConfiguration()
         ↓
    Reads from email_config or falls back to .env
```

This visual documentation helps understand the complete email configuration system architecture!
